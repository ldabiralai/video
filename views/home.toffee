<ul id="video-list">
{#
  for video in videos
    data = video.title.toLowerCase()
    {:
      <li data-title="#{data}">
        #{video.title}
        {# if video.duration {:
           - #{video.duration} minutes
        :} #}
      </li>
    :}
#}
</ul>
